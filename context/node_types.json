{
    "webhook": {
        "name": "Webhook Trigger",
        "type": "trigger",
        "description": "Triggers a workflow when an HTTP request is received at the specified endpoint"
    },
    "code": {
        "name": "Code Action", 
        "type": "action",
        "description": "Executes custom JavaScript code to transform or process data"
    },
    "httpRequest": {
        "name": "HTTP Request",
        "type": "action", 
        "description": "Makes HTTP requests to external APIs and services"
    }
}
